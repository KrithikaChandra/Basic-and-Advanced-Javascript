export const ButtonStyles = {
  primary: {
    color: "white",
    background: "blue"
  },
  secondary: {
    color: "black",
    background: "gray"
  }
};

export default function Button(label) {
  return `Button: ${label}`;
}