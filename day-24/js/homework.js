// Practice tasks before next class · Advanced Day 12

//1. Take a folder of three small modules and write a barrel index.js for it. Use both named re-exports and export *.

//2. Add a dynamic import() for a feature in a small Vite app. Open DevTools Network tab and watch the chunk load only when triggered.

//3. Build a tiny circular dependency on purpose (a.js↔b.js with a value used at top level). Observe the bug. Refactor to fix it.